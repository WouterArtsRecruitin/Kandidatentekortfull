# Claude Integration Test

This is a test file to verify Claude Code Review is working.

## Test Function

```typescript
function greet(name: string): string {
  return "Hello, " + name;
}
```

This simple function should trigger Claude to review the PR.
